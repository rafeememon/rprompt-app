apply from: '../gradle/java.gradle'

dependencies {
  compile project(':rprompt-backend')
  compile project(':rprompt-frontend')
}

task run(type: JavaExec) {
  classpath = sourceSets.main.runtimeClasspath
  main = 'com.rbase.rprompt.dropwizard.RPromptApplication'
}
